# Copy to the backend repo as env.shared (non-secret values only).
# Secrets belong in AWS Secrets Manager.

APP_ENV=staging
LOG_LEVEL=INFO
FEATURE_NEW_DASHBOARD=true
SESSION_COOKIE_SECURE=true
